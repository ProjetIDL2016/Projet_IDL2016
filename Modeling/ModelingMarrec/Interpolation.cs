using System;
using MathHelper;

public class Interpolation
{
    float resInterpolation = 0;
    public static double interpolation(double u, double v)
    {
        resInterpolation = MathHelper.Lerp();
        return resInterpolation;
    }
}
